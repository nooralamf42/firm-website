export const metadata = {
  title: 'Pricing | Innovative National Tax',
  description: 'Simple, transparent pricing plans tailored to your accounting and bookkeeping needs.',
};

export default function PricingLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
